# 작업 내역

> 기간: 2026-09-04 ~ 2026-09-10
> 담당: 황인성

---

## 1. 드론 — 교전 관측 이동 (스플라인 위 자동 관측 지점 선정)

### 목적
교전이 시작되면 드론이 정지비행하며 짐벌만 돌리던 것을, **스플라인 위에서 "지금 전황을
가장 잘 보여주는 지점"으로 스스로 이동**하도록 확장한다. 짐벌은 각도만 바꿀 뿐 거리를 못
줄여서, 교전이 2·3차 전투지로 옮겨가면 "전원이 담기긴 하는데 아무것도 분간이 안 되는" 그림이
남던 문제를 해결한다.

### 결과

**설계 원칙**: 이 드론에는 지형 회피가 없어 자유비행 대신 "아티스트가 그려준 선 위에서 가장
잘 보이는 지점"을 고르는 구조로 감 — 안전이 저작 단계에서 보장된다. 역할 분담: 폰이 화각/부감
제약을 만족하는 관측 지점을 정하고, 자율비행은 그 스플라인 진행거리까지 부드럽게 날아가는
것만 책임진다.

**겪은 함정 8건(시간순)**:
- **대리 지표로 목표 함수를 잘못 세움**: "내가 정한 이상적 거리·고도에 얼마나 가까운가"로
  짰다가, 실제 대상 배치(퍼진 정도, 화각 포함 여부)를 안 봐서 11명이 300m로 흩어져 있든 20m에
  뭉쳐 있든 점수가 같았음. 사용자 지적으로 **실제 프레이밍 결과**(그 지점에서의 필요 화각·
  조준 부감)로 갈아엎음.
- **연속 최적화가 래칫이 됨**: "가장 점수 좋은 지점"을 계속 찾게 했더니 3분 반 동안 25m씩
  21회 뒤로 기어감(연속 점수엔 항상 "조금 더 나은 점"이 존재). **제약 만족**(조건을 만족하는
  동안은 후보 탐색 자체를 안 함) 방식으로 재설계해 안정성을 튜닝이 아니라 구조로 확보.
- **클램프된 화각으로 초과량을 판정**: 초과 벌점이 클램프 이후 값으로 계산돼 항상 정확히
  0이었음 — 화각이 터졌는데도 "완전 만족"으로 오판. 클램프 전 원본을 따로 반환하도록 수정.
- **판정 문턱이 하나뿐이라 경계에서 진동**: 부감이 -43~-45°를 오가는데 밴드 경계가 정확히
  -45°라 1초마다 만족/불만족이 뒤집힘. 슈미트 트리거로 분리(목적지 선정은 엄격, 유지 판정은
  느슨하게).
- **TargetDetection을 프레이밍 입력으로 써서 순환 의존 발생**: 줌아웃→탐지 해제→프레이밍
  축소→줌인이 반복되고, 5명 중 1명만 잡혀도 나머지 4명이 영영 프레이밍에서 빠짐. 자동 정찰은
  센서 시뮬레이션이 아니라 연출이므로 레지스트리에서 실제 위치를 직접 읽도록 전환.
- **`MoveDroneToPath`가 경로 끝까지 주행**: 경로가 "시작점=트럭, 끝점=낙하산"이라 끝까지
  날아가면 방금 떠나온 낙하산 자리로 되돌아감 — 실측 630m에서 994m까지 24초간 역주행하는
  동안 1차 전투가 끝남. 이미 관측 중이면 경로 주행 없이 "레일만 갈아탄다"는 개념으로 수정.
- **시나리오 스텝을 월드 상태 대신 믿음**: "3분대 제외 전멸→지휘소만 프레이밍" 조건이 실제로는
  월드 상태인데 스텝 발동 타이밍에 맡겨서, 2차 전투지에 적 8명이 살아있는데도 세트가 3으로
  넘어가 그 8명을 버리고 530m를 날아감. 상태로 게이트하도록 수정.
- **UGV도 RCWS를 달고 있음**: "트럭 RCWS가 교전을 시작했는가"를 세트와 무관하게 검사해서
  1차 전투지에서 UGV 자동조준이 래치를 걸어버림 — Zone3에서만 검사하도록 제한.

**비행 유도 — 구조적 결함 3연쇄**: 관측 이동이 계속 "덜컹/울렁"거려 하나씩 벗겨냄. ① 속도
적분기가 가속도 상한 포화를 모른 채 계속 쌓여(안티와인드업 누락) ±11.5° 기울기가 3~5초
주기로 진동 — 조건부 적분(포화가 더 심해지면 갱신을 버림)으로 수정. ② 그 수정을 수평 포화
하나로 3차원 전체에 적용해 반대 방향으로 틀림 — 수평(XY 벡터 크기)/수직(Z 절대값)을 각자의
상한으로 따로 판정하도록 분리. ③ 그래도 속도가 사인파로 출렁임 — 캐스케이드 특성방정식을
풀어 ζ≈0.4(내부 루프 지연 포함)의 저감쇠 진동임을 확인, 원인은 **경로 추종은 거버너 속도를
그대로 써서 이미 피드포워드 구조인데 관측은 위치 오차만으로 속도를 짜내는 비대칭**이었음 —
관측에도 속도 피드포워드를 추가하고 적분을 0.15배로 줄여(ζ 0.51→0.68) 해결. ④ 그래도 5초
주기로 턱턱거림 — 필요 화각이 계속 84~90°(최대치 근처)라 히스테리시스가 무력화돼 매 평가마다
"덜 나쁜 곳"을 새로 고르고 있었음 — 도주 중인 적을 트래킹에서 제외 + 차선 모드 개선 마진
도입으로 해결.

**카메라와 기체 방향 분리**: "짐벌 요 여유 확보"라는 전제가 틀렸음을 발견 — 짐벌 요는 클램프가
없어(`Fmod`만) 기수가 어디를 보든 카메라는 대상을 물 수 있는데, 기수를 계속 대상 쪽으로
돌리다 옆으로 수백 미터를 날며 방위각이 빠르게 쓸려 기체가 빙글빙글 돎. 사용자 지적대로
기수를 래치하고 8°/s 상한 안에서만 돌리게 해 급회전을 구조적으로 차단, 짐벌(슬루 25°/s)이
여유 있게 상쇄.

**곁가지로 잡은 탐지 버그**: 교전 중 적군 탐지 박스가 하나둘 사라지던 원인 — 폰의 미러
프로퍼티(`DetectionMinScreenSizeFraction`)가 `BeginPlay`마다 컴포넌트 설정을 덮어써서
사용자가 꺼둔 값(0)이 0.04로 되돌아가고 있었음. 이 필터가 거리가 아니라 화면 점유율 기준이라
화각에 반비례 — 짐벌이 88°로 광각일 때 유효 탐지거리가 800m 설정과 무관하게 45m로 무너짐.
폰의 미러를 제거해 컴포넌트 프로퍼티가 유일한 기준이 되게 함.

**남은 것**: 도주 시작 장면이 대상 33개·화각 84° 프레이밍이라 점으로 보임(별도 결정 필요),
경로 고도 부족(부감 -30~-40°대, 선호 범위 -45~-60° 미달), 2프로세스 실환경 검증.

**변경 파일**: `Source/titan_example/Drone/DroneAutopilotComponent.h/.cpp`,
`Drone/DronePawn.h/.cpp`, `UI/ScenarioStateSubsystem.cpp`.

---

## 2. 드론 — 수동 조종 비행/짐벌 축 분리

### 목적
자체방호축 수동 조종이 `UAV` 하나로 비행+짐벌을 한 덩어리로 넘겨받던 것을, "기체는 자율비행/
교전 관측을 계속하고 사람은 카메라만 돌려본다"가 가능하도록 두 축으로 분리한다. 전시에서
가장 많이 쓰이는 조합.

### 결과

폰의 수동 상태를 bool에서 모드 enum(`None`/`GimbalOnly`/`Full`)으로 교체 — 효과를 거는
쪽에서는 "자율비행을 놓았나"/"짐벌을 자동이 안 잡나" 두 질문으로 나눠서 봄.

**겪은 함정 4건**: ① 짐벌 자동 로직(`UpdateGimbalWideEngagementView`) 안에 관측 지점
선정(비행 몫)이 섞여 있어서, "수동이면 통째로 건너뛴다"를 그대로 두면 카메라만 넘겨받았는데
기체가 제자리에 굳음 — 함수 중간에서 갈라 비행 몫은 항상 돌게 함. ② 짐벌 입력이
`GimbalReconPhase==Idle`일 때만 통과하는 오래된 가드(자동 슬루와 스틱이 다투는 걸 막으려던
것)에 걸려, 정작 짐벌을 돌리고 싶은 교전 구간(항상 `WideEngagementView`)에서 입력이 통째로
버려짐 — 수동 모드에선 다툴 상대가 없으므로 단계 무관 통과로 수정. ③ **가장 오래 헤맨 것**:
짐벌 4방향 버튼이 비행 전용 IMC 안에 있어 "전체 수동일 때만" 그 IMC가 올라가고 있었음 —
상태 기계 로그는 정상으로 찍혀 헤맴, 짐벌 전용 모드에서도 IMC를 올리도록 수정(같이 올라오는
비행 액션은 다른 조건에서 걸러짐). ④ 그런데 IMC를 올리면 메인 스틱이 짐벌 IMC 액션에 우선
바인딩돼 죽음 — 짐벌 전용 모드에서는 그 스틱을 짐벌 pan/tilt로 직접 돌리도록 재배선.

**부호 사슬 — 같은 물리 축을 두 용도로 쓰는 대가**: 같은 조이스틱 축(`Axis_1`)을 비행과
카메라 두 용도로 나눠 쓰는데 서로 원하는 극성이 반대라 두 번 왕복. 기존 `bInvertPitchAxis`
하나로는 못 풀어서(빙의 경로와 라우팅 경로가 서로 다른 IMC를 쓰고 그 IMC들의 Negate 여부가
다름) 라우팅 경로 전용 반전 노브 2개를 신설.

**부수 발견 — 수동 해제 시 576km/h 역주행**: 수동으로 날리다 끄면 출발 위치로 초고속
역주행 후 자율비행을 다시 시작하던 버그. 원인은 수동 진입 시 `Autopilot->Disengage()`가
`State`를 `Idle`로 만드는데, 해제 시 이를 "지상에서 새로 시작"으로 오판해 이륙 단계로
들어가고, 이륙 속도 상한을 스플라인 첫 포인트까지의 거리(800m)에서 역산해 576km/h가 나온
것. `ResumeAfterManualControl`을 신설해 이륙을 건너뛰고 현재 위치를 경로에 투영해 이어받게
함. 사용자가 "교전 트래킹에도 같은 버그가 있을 수 있다"고 짚어줘서 확인한 결과 **두 겹
버그**였음이 드러남 — 관측(`Observing`) 상태까지 잃고 경로 끝(낙하산 자리)까지 주행하는
문제가 추가로 있었음, 수동 진입 시 관측 여부를 `Disengage` 전에 스냅샷해뒀다가 복귀 시 되돌려
해결.

**짐벌 기본 자세 복귀**: 사용자가 먼저 제안한 "스켈레탈 메시 본을 돌려 기본각을 바꾼다"는
방법은 `GimbalPitchDeg`가 바인드 포즈로부터의 누적 오프셋이라 문제를 안 푼다는 것을 확인 —
"0이 어디냐"만 바뀌고 사용자가 돌려놓은 값은 그대로 남아 복귀가 안 됨. 코드 파라미터
(`GimbalHome*`)로 넣고, 아무도 짐벌을 안 잡는 유일한 구간(정찰 단계 Idle)에서만 돌게 함.

**변경 파일**: `Drone/DronePawn.h/.cpp`, `Drone/DroneAutopilotComponent.h/.cpp`,
`titan_examplePlayerController.h/.cpp`.

---

## 3. 인게임 Settings — Graphics 탭 구현

### 목적
09-03 조사에서 확정한 스코프(품질 프리셋 + 카메라 캡쳐 주기 + 렌더링 방식 + 나무 성능)를
실제로 구현한다. 핵심 동기: `sg.*` cvar 12개가 하드코딩돼 있어 런타임 품질 변경이 완전
no-op이었던 것을 근본 해결.

### 결과

**구조**: 신규 `UTitanGraphicsSettings`(`UDeveloperSettings`, `defaultconfig`,
`Config/DefaultGame.ini`)를 단일 소스로 — 값이 P4로 공유되고 패키징에 포함되며, 에디터/PIE/
패키지가 같은 파일 하나를 읽어 "에디터는 A인데 패키지는 B"가 구조적으로 불가능해짐(엔진
기본은 에디터/게임이 서로 다른 ini를 읽어서 갈라짐). 적용 시점은 `OnPostEngineInit`(스케일러빌리티
로드 이후). cvar가 아닌 값(캡쳐 주기·식생)은 컴포넌트 프로퍼티라 자동 반영 경로가 없어 월드
BeginPlay 훅에서 액터를 순회해 직접 대입.

**ini 이관 결과**: `sg.*` 12개를 `WindowsEngine.ini`에서 `UTitanGraphicsSettings`로 이관
(우선순위가 런타임 변경을 막고 있었음), Lumen/VSM 그룹 멤버 10개는 `DefaultScalability.ini`
프리셋 재정의로(엔진 프리셋 위에 키 단위 병합이라 현재 쓰는 단계만 채우면 기본 화면이 동일),
Lumen 비그룹 값 6개는 Windows 전용 ini에서 공통 `DefaultEngine.ini`로(**Linux 빌드가 이 튜닝을
하나도 못 받고 있었음** — LIG 납품물이 Linux 패키지라 중요), 반사 방식은 Windows=SSR/
Linux=Lumen으로 갈려 있던 것을 SSR로 통일. 실측: 이관 후 `DumpCVars sg.` 12개 전부
`Scalability`로 풀림, PIE의 "거부됨" 경고가 12줄→2줄로 감소(남은 2줄은 VRAM 안전장치라
의도적 고정).

**탭 항목 — 헤더 5 + 항목 18**: 전체 품질/그림자/GI/반사/텍스처/시야거리/이펙트/셰이딩/
포스트프로세스(드롭다운 4단계), 안티에일리어싱 품질·방식, 반사 방식(SSR/Lumen/끔), VSync,
**나무 바람 흔들림 거리(0~200m)/LOD 전환 배율(0.1~2.0)을 숫자 입력(USpinBox)**으로, 카메라
피드(CCTV/드론/전장) 갱신 주기 3종. 나무 2항목만 드롭다운이 아니라 숫자인 이유는 연속량이고
실측 성능 곡선이 있어 4단계로 쪼개면 정보를 버린다는 사용자 지적을 반영한 것 — 기본값(30m/
0.5)이 08-22 성능 최적화 문서의 실측 knee 값과 일치. "초목 품질"(`sg.FoliageQuality`)은 이
레벨에서 효과가 0임을 확인하고 제거(우리 나무는 PCG로 뿌린 뒤 레벨에 구워진 ISM/HISM이라
런타임 생성 PCG에만 영향을 주는 그룹이 무의미).

**되돌리기 버튼이 필요한 이유**: 품질을 한 번이라도 바꾸면 그 PC의 `GameUserSettings.ini`에
`[ScalabilityGroups]`가 생기고, 그 뒤로는 `Config/DefaultGame.ini`를 고쳐도 반영이 안 됨(프레임
상한 때 겪었던 것과 같은 함정) — `GraphicsResetButton`이 두 섹션을 지우고 프로젝트 기본값을
재적용.

> **트러블슈팅 - `UDeveloperSettings` 상속 시 컴파일은 통과하는데 링크에서만 터짐**
>
> `DeveloperSettings`가 엔진 `Build.cs`의 `PublicDependencyModuleNames`에 있는데도 링크
> 에러가 났다. 헤더가 `PublicIncludePathModuleNames`에도 등록돼 있어 include는 되지만 API
> export 매크로가 안 깔린 상태였던 것 — 우리 프로젝트 `Build.cs`에 모듈을 명시해서 해결.

**WBP 함정**: Overlay에 컨트롤을 `Fill`로 두면 라벨을 덮고 `Right`로 두면 콘텐츠가 쪼그라듦
(둘 다 실제로 겪음) — 컨트롤이 폭을 차지해야 하는 행은 HorizontalBox+SizeBox로 재설계.
라벨 열은 반드시 `Fill`이어야 `AutoWrapText`가 동작(`Auto`면 무한 폭을 요구해 컨트롤을
밀어냄). **액터 존재 여부로 UI를 비활성화하면 안 됨** — 드론/전장 캡쳐 주기 항목을 월드에
액터가 있을 때만 활성화했더니, 차량이 없는 `kadex_lobby`(정작 설정을 하라고 만든 화면)에서
둘 다 비활성이 됨. `memo.md`를 요구사항 근거로 인용했다가(`r.ReflectionMethod=3`의 의도를
찾던 중) 최신화 없는 개인 스크래치 메모라는 걸 뒤늦게 확인 — 의도가 불명확하면 추측하지 않고
선택지를 제시하는 방향으로 정정.

**검증 상태**: 빌드 통과, WBP 2개(헤더+행) 정상 동작, `kadex_lobby`에서 훅 자체 정상(차량/나무가
없어 0개가 정상값). **`New_kadex_0811`에서 실제 대상 탐색(ISM 17개/QuadCam 2개/Drone 1개/
Truck 1개) 확인은 대기.** VSync는 에디터에서 구조적으로 안 먹어 패키지에서만 확인 가능,
Linux 패키지 룩(반사 Lumen→SSR 전환)은 실측 필요.

**변경/신규 파일**: `Settings/TitanGraphicsSettings.h/.cpp`(신규),
`Settings/TitanGraphicsSubsystem.h/.cpp`(신규), `UI/GraphicsSettingRowWidget.h/.cpp`(신규),
`UI/GameSettingsWidget.h/.cpp`, `Plugins/QuadCamModule/.../QuadCamComponent.h/.cpp`
(`CaptureEveryNTicks`), `Config/DefaultScalability.ini`(신규), `Config/DefaultEngine.ini`,
`Config/Windows/WindowsEngine.ini`, `titan_example.Build.cs`.

---

## 4. UI — 축 선택 화면(kadex_lobby) 운영자 UX 개편

### 목적
패키지 실행 시 처음 뜨는 축 선택 화면이 "무엇을 눌러야 하는지 실행가이드 문서가 설명하는"
상태였던 것을, **화면 자체가 설명서 역할을 하도록** 개편한다. 조작 대상은 관람객이 아니라
LIG 측/전시 세팅 담당 인력.

### 결과

**Solo와 Demo는 별개 개념 — 사용자 지적으로 정정**: Solo(호스트/클라 없이 이 PC 하나로
여는 것, `?Listen` 유무가 결정)와 Demo/FullSystem(조준·사격을 누가 쥐는가, `RunMode`가
결정)이 별개인데 초판 문서가 엮어서 서술했다가 정정. `StartSoloAxis(Axis, bDemoMode=true)`의
기본값이 true인 게 혼동의 원인이었을 뿐, Solo가 생긴 진짜 이유는 데모가 아니라 축 선택 편의였음
(`New_kadex_0811`의 `DefaultAxisWhenUnspecified=UGV`라 자체방호축으로 들어가려면 원래 다른
PC에서 Host를 띄우거나 콘솔 명령이 필요했던 것을 버튼으로 옮긴 것). 코드로 확인한 사실 3가지도
정리 — 리슨서버는 클라 없이 싱글플레이처럼 동작(설계 문서에 이미 명시), `StartSoloAxis`
경로에는 애초에 클라가 붙을 수 없음(`?Listen` 없으면 net driver 자체가 안 생김), 자체방호↔
상위체계 연동은 현재 코드에 존재하지 않음(자체방호 PC가 외부와 주고받는 건 RTSP 송출뿐).

**화면 방향 확정**: 실제로 쓰는 경로는 UGV 시뮬레이터(Host)/이동형지휘소(Client) 둘뿐 — 이
둘을 "필요한 입력 1칸 + 시작 버튼 1개"의 완결된 카드로 만들고 나머지(데모 모드, 포트, 해상도,
Solo)는 "고급 실행 옵션"으로 접음. 프리셋 카드 3~4장을 만들었다가 실제 경로가 둘뿐이라 오히려
복잡해져서 걷어낸 시행착오도 있었음.

**구현(C++만, `.uasset` 무변경) 5가지**:
- **엔터 커밋 함정 제거**: 각 버튼 클릭 진입부에서 모든 텍스트 필드를 강제로 다시 읽어, 타이핑만
  하고 엔터 없이 버튼을 눌러도 그 값으로 시작되게 함.
- **입력값 ini 저장**: `SaveConfig()` 호출이 프로젝트 전체에 0건이었던 것을 발견, 두 서브시스템
  끝에 추가 — `Saved/Config/<Platform>/Game.ini`에 남아 다음 실행에 재사용됨.
- **"이 PC의 IP" 표시 — 라우팅 테이블에 묻는 방식**: UDP 소켓으로 상대 IP에 `Connect()`(패킷은
  안 나감) 후 `GetAddress()`를 읽어 OS가 실제로 쓸 출발지 IP를 얻음. 어댑터 열거
  (`GetLocalAdapterAddresses()`)를 주 수단으로 안 쓴 이유는 플랫폼별로 결과가 다르기
  때문(윈도우는 Tailscale/VPN 주소가 안 나오고 리눅스는 나오는 등, 엔진 소스로 확인) — 어댑터
  열거는 폴백으로만 사용.
- **툴팁 도움말**: 코드가 `NativeConstruct`에서 모든 입력칸/버튼에 자동으로 심음 — WBP에서
  18개 칸에 일일이 타이핑할 필요가 없고, 문구가 "그 값의 의미를 아는 코드"에 살아 계약이
  바뀌면 같이 눈에 든다.
- **실행 전 점검**: 확실히 실패할 입력만 차단(포트 범위 밖, 수신 포트 충돌, Client인데 IP
  공백)하고, 별도 버튼으로 이 PC의 IP 근거/입력된 상대 주소로의 경로 유무/포트 상태를 보고
  (방화벽·상대편 상태는 확인 못 한다고 명시).

**WBP 함정 3개**: 바인딩되는 위젯은 전부 같은 WidgetTree 안에 있어야 함(자식 UserWidget으로
빼면 `BindWidgetOptional`이 못 찾음), 텍스트 입력은 `Editable Text`만 가능(`Editable Text Box`는
이 엔진 버전에서 `Internal Compiler Error`가 100% 재현됨), 부모 클래스를 반드시
`AxisSelectionWidget`으로 지정해야 함(`UserWidget`으로 남아 있으면 이름이 다 맞아도 모든
바인딩이 조용히 null — 08-20 "UGV→RC 무응답 버그"의 진짜 원인이었음).

**남은 것**: 빌드+PIE 확인, WBP 작업(사용자), 실행가이드 문서 갱신은 실측 완료 후로 보류
(검증 안 된 동작을 고객 문서에 먼저 쓰지 않기 위함).

**변경 파일**: `UI/AxisSelectionWidget.h/.cpp`.

---

## 5. UI — 미니맵 트럭 마커 회전 버그 + 대시보드 그래프 스무딩

### 목적
미니맵의 트럭 방향 화살표가 90도 어긋나는 버그를 잡고, 고도/속도 그래프의 렌더 아티팩트와
데이터 노이즈를 개선한다.

### 결과

**미니맵 트럭 마커 90도 회전 버그**: RCWS 방향/FOV 콘 마커는 정상인데 트럭 방향 화살표만
반시계로 90도 어긋남. 원인은 2026-08-02에 넣어둔 고정 보정값(`TruckMeshForwardOffsetDeg=-90`)이
그 사이 `BP_TitanTruck`의 컴포넌트 구성이 바뀌면서(당시엔 마운트/카메라 쪽에 있던 -90 오프셋이
지금은 `BodyMesh` 쪽으로 옮겨감) 중복 보정이 된 것 — `GetActorRotation().Yaw`가 이미 시각적
정면과 일치하는 상태에서 추가로 -90을 더 돌리고 있었음. FOV 콘이 멀쩡했던 이유는
`GetSightWorldRotation()`이 부착 체인을 전부 풀어 월드 회전을 구해서 BP 구조 변경에 영향을
안 받기 때문 — "마커는 틀렸는데 콘은 맞는" 증상이 보이면 액터 정면 기준점 쪽부터 의심할 것을
교훈으로 기록. 동일 코드가 복사된 세 파일(`Monitor1Widget`/`SelfDefenseMonitor1Widget`/
`SelfDefenseDashboardWidget`)에서 오프셋 변수 제거, `guide/ui_dev_guide.md`에 "보정 없이
`GetActorRotation().Yaw` 사용"을 명시해 재발 방지.

**LineGraphWidget 곡선 후처리**: 고도/속도 그래프가 (1) 구간마다 선 두께가 달라 보이고 (2)
잘게 삐죽거리는 두 가지 서로 다른 문제. ① 두께 불균일은 데이터가 아니라 렌더 아티팩트 —
`HistoryLength=300`(30Hz×10초)인데 화면상 90px 남짓이라 픽셀당 정점 3~4개, `MakeLines`가
세그먼트마다 그리는 AA 쿼드가 겹쳐 알파가 누적되는 것. ② 삐죽거림은 Chaos 강체 속도를
필터 없이 30Hz로 그대로 히스토리에 쌓아서 로터 진동이 실린 것. 해결: 픽셀 버킷 리샘플(2px당
정점 1개 평균) + Fritsch-Carlson monotone cubic 보간(Catmull-Rom은 오버슛으로 그라데이션
채우기가 위젯 밖으로 삐져나가는 문제가 있어 기각) + 히스토리 저장 시 3샘플 중앙값(스파이크
제거)과 프레임률 독립 1극 저역통과 필터. 순서가 중요(EMA 저장 → 버킷 평균 그리기 → 스플라인
보간 사이 — 뒤집으면 안 됨). 영향 범위는 `SLineGraph`를 쓰는 모든 위젯(`Monitor1Widget`/
`StatusHUDWidget` 포함). 곁들여 세로축 상한 실측 — 속도 그래프 상한(75km/h)이 순항속도
70km/h 대비 빡빡해 90 정도로 올리는 게 적당하다는 근거 데이터도 정리(동적 오토 레인지는
다음 작업 설계로 남김).

**변경 파일**: `UI/Monitor1Widget.cpp`, `UI/SelfDefenseMonitor1Widget.cpp`,
`UI/SelfDefenseDashboardWidget.cpp`, `UI/LineGraphWidget.h/.cpp`, `UI/StatusHUDComponent.h/.cpp`.

---

## 6. UGV — 서스펜션 승차감 튜닝

### 목적
`New_kadex_0811`의 작은 바위 액터에 콜리전을 켜서 주행 중 자연스러운 덜컹거림을 만든 뒤,
충격이 너무 날카로워진 것을 서스펜션 재조정으로 완화한다. 09-02 신규 모델 교체 때의 서스펜션
값은 휠 개수 스케일 규칙(×16/6)으로 기계적으로 환산한 것이라 승차감을 본 적이 없었다.

### 결과

**핵심 구조 규명 — Chaos 5.8 서스펜션은 경로가 셋으로 갈린다**: 차체가 실제로 튀는 정도는
`FPBDSuspensionConstraints`(PBD 컨스트레인트, `SpringRate`/`SuspensionDampingRatio`/
`SuspensionMaxRaise/Drop`을 읽음), 그립은 `FSimpleSuspensionSim`(차체를 밀지는 않고 휠 하중만
결정), 바퀴 비주얼은 `GetSuspensionOffset`(`SuspensionSmoothing`만 읽음) — 서로 다른 코드가
같은 프로퍼티를 다르게 해석한다. 힘 기반 경로(`AddForceAtPosition`)는
`p.Vehicle.DisableConstraintSuspension` 전용이라 이걸 읽고 튜닝 판단을 하면 틀린다.

> **트러블슈팅 - 죽어 있는 값 2개 발견**
>
> `SpringPreload`는 솔버 소스에서 `/*+ Setting.SpringPreload*/`로 **주석 처리**돼 있어 UE
> 5.8에서 어떤 경로로도 동작하지 않음 — 09-02에 180→450으로 스케일 환산했던 게 효과 0이었음
> (엔진이 나중에 주석을 풀 가능성을 감안해 값 자체는 남겨둠). `RollbarScaling`도 죽어있음 —
> 축이 휠 클래스 포인터 기준으로 묶이는데 UGV_0901은 6륜이 전부 같은 휠 클래스라 6륜짜리
> 축이 1개만 만들어지고, 롤바 코드가 "축당 휠 2개"만 처리하는 가드에 막혀 통째로 스킵된다 —
> **이 차량은 안티롤바가 0**이다(살리려면 휠 클래스를 앞/중/뒤 3개로 복제해야 함, 이번엔
> 안 함).

`MaxRaise`와 `MaxDrop`이 대칭이 아니라는 것도 확인 — 압축량이 `MaxLength(=MaxDrop 기준)-
Distance`로 계산되므로 **`MaxRaise`는 차고를 안 바꾸고 "바닥 치기 전 여유"만 늘림**(기존
12cm는 12cm 넘는 돌에서 트래블이 끝나 충격이 감쇠 없이 차체로 직행하던 상태). `SuspensionSmoothing`은
물리에 전혀 닿지 않고 비주얼 전용(피직스 스레드 이동평균 + 게임 스레드 지수보간이 겹쳐
걸리며, 바퀴 본의 시각적 가동 범위를 `[-MaxDrop,+MaxRaise]`로 자름).

**최종 값**(`BP_UGV_Wheel_0901`): `SpringRate` 900→200(4.5배 무르게, 사용자가 실주행으로
직접 잡음), `SuspensionMaxRaise` 12→16(압축 여유 확보, 차고는 불변), `SuspensionSmoothing`
0→5(바퀴 본 떨림 억제). `SpringPreload`/`RollbarScaling`은 죽은 값이라 유지. 후속 후보:
`SuspensionDampingRatio` 0.7→0.45~0.5(아직 날카로우면), 휠 클래스 3분할(롤바 살리기 + 축별
스프링 분리).

**측정 방법**: `SetSuspensionParams()`가 BlueprintCallable이라 PIE 중 에디터 재시작 없이
값을 바꿔가며 감을 잡을 수 있음. `p.Vehicle.ShowSuspensionLimits/Forces/Raycasts`로 디버그
드로우. ⚠ 강성이 `Dt²`에 비례해 프레임률에 의존적 — 60fps에서 잡은 느낌이 30fps에서는 강성
4배가 되므로 `FrameRateLimit=60`이 실제로 걸린 상태에서 튜닝해야 함.

**변경 파일**: `BP_UGV_Wheel_0901`(에셋).

---

## 7. 적군 AI — 애니메이션 분리(적군 블렌드스페이스 신설) + 도주 자세 잔류 버그

### 목적
디자이너가 적군 스켈레톤을 수정하고 애니메이션을 아군과 분리하면서 깨진 참조를 복구하고,
도주 중 이전 전투 자세가 남아 기울어진 채 달리는 버그를 해결한다.

### 결과

**아군/적군 애니메이션 분리 사고 복구**: 디자이너 CL 3건(452/454/455)이 적군 스켈레톤을
수정하고 애니메이션 30개를 `AS_Enemy/`로 분리한 뒤 접두사를 떼는 리네임 과정에서, **리다이렉터가
루트에 남고 그 대상이 삭제되어 참조 사슬이 끊김** — `BS_ally_kadex`(방향 샘플 27칸 중 13개
유실), `BS_ally_kadex_Crouch`(걷기·뛰기 12개 전부 유실)에서 로드 에러 발생. 파일 개수만
보면 줄지 않아서(106→136) 헷갈리기 쉬웠으나, 새로 생긴 55개 중 22개가 1.4~1.6KB짜리 리다이렉터
껍데기였고 실물은 아군 44/적군 40/기타 30개뿐이었음(`.uasset` 크기 또는 스켈레톤 참조로
실물 여부를 판별해야 함).

조사 결과 **아군 애셋은 "옮겨간" 게 아니라 애초에 아군 스켈레톤 실물이 그만큼 없었음** —
디자이너 확인 결과 아군은 새 스켈레톤(`soldier_T_Skeleton`)으로 작업 진행 중이라 시퀀스가
이후 입고 예정, 아군 쪽은 복구하지 않고 대기하기로 결정. 적군은 애셋 자체는 정상이었으나
ABP가 여전히 깨진 아군 블렌드스페이스를 물고 있어서 아무도 못 쓰는 상태였음 — 적군 전용
블렌드스페이스 2종(`BS_enemy_kadex` 27샘플, `BS_enemy_kadex_Crouch` 22샘플)을 신설하고
`ABP_Enemy_kadex2`/`BP_Enemy_Base` 슬롯을 재배선해 PIE 확인 완료. 적군 전용 애셋이 아직
없는 부분(숙인 정지, 엎드리기 계열)은 `Enemy_Skeleton`의 `CompatibleSkeletons`에 아군
스켈레톤이 등록돼 있어 호환 재생으로 임시 커버.

**도주 중 자세 잔류 버그**: 적군이 2·3차 전투지로 도망갈 때 몸이 좌/우로 기울어진 채 달리는
증상. 원인은 `LeanAlpha`를 쓰는 곳이 `ApplyCombatPose()` 단 한 곳뿐인데 이 함수가
`Combat` 상태에서만 매 틱 호출됨 — 도주 커밋 시점(`CommitPendingFlee()`)은 상태 변수만
리셋하고 애니메이션 플래그는 그대로 둬서, 도주 직전 프레임의 자세가 도주 내내 그대로 굳음.
같은 원인으로 `IsHoldingWeapon?`(Cover 포즈에서 커밋되면 총 내린 채 발사), `IsProne`(엎드린
자세로 질주) 등도 같이 잘못될 수 있었음. `CommitPendingFlee()`에서 도주 자세(Lean=0,
Prone=false, Kneeling=true, HoldingWeapon=true)를 1회 확정하도록 수정.

> **교훈(문서에 직접 기록)**: "매 틱 쓰는 상태값"과 "상태 전환 시 한 번만 쓰는 상태값"이
> 섞여 있고 어떤 최상위 상태에서는 그 매 틱 갱신이 아예 안 도는 구조가 원인 — 같은 패턴을
> 이미 `AimPitch`(경계 이동 중 상체를 들어올린 채 교전 시작해도 아무도 안 되돌려 위를 본
> 채 굳던 문제)에서도 겪었다. 새 최상위 상태나 전환 경로를 추가할 때는 "이 상태에서 매 틱
> 갱신되지 않는 애니메이션 플래그가 뭐가 남는가"를 훑을 것.

**변경/신규 파일**: 신규 `BS_enemy_kadex`/`BS_enemy_kadex_Crouch`(에셋),
`ABP_Enemy_kadex2`(배선), `Soldiers/EnemyCombatComponent.cpp`(`CommitPendingFlee`).

---

## 기타 메모

- 이번 주에도 애셋 참조 관련 사고가 두 건(아군/적군 애니메이션 리다이렉터, MCP `set_parent`류
  저장 누락과 같은 계열) 있었다 — 리네임/이동 후에는 리다이렉터 정리를 습관화할 필요.
- Graphics 설정 탭·축 선택 화면 UX 개편 둘 다 "실측/사용자 검증 대기" 상태로 마무리 —
  다음 주 초반에 `New_kadex_0811` 진입 확인과 WBP 작업 완료 여부를 먼저 확인할 것.
